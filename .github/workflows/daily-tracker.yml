name: Open-VSX Download Tracker

on:
  # 测试定时任务 - 临时设置为10:50运行
  schedule:
    - cron: '50 2 * * *'  # 测试用：UTC 2:50 AM = 北京时间 10:50 AM（约13分钟后）
  
  # 允许手动触发
  workflow_dispatch:

# 设置工作流权限
permissions:
  contents: write  # 允许推送代码

jobs:
  track-downloads:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true
      
      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 下载历史数据
        continue-on-error: true
        run: |
          # 从上一次运行中恢复历史数据
          if [ -f download_history.json ]; then
            echo "找到历史数据文件"
          else
            echo "未找到历史数据文件，将创建新文件"
          fi
      
      - name: 运行下载量追踪器
        env:
          EXTENSION_NAMESPACE: ${{ secrets.EXTENSION_NAMESPACE }}
          EXTENSION_NAME: ${{ secrets.EXTENSION_NAME }}
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
          RECEIVER_EMAIL: ${{ secrets.RECEIVER_EMAIL }}
        run: |
          python tracker.py
      
      - name: 提交历史数据
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add download_history.json
          git diff --quiet && git diff --staged --quiet || git commit -m "📊 Update download history - $(date +'%Y-%m-%d')"
          git push

